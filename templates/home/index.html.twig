{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Hello {{ user.firstName }}  {{ user.lastName }}</h1>
    </div>
    <div>
        <a href="{{ path('app_logout') }}" >Logout</a>
    </div>
{% endblock %}